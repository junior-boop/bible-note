import type { Notes } from "../../lib/livestore/schema";

export default function NoteItems({ data }: { data: Notes }) {
    // console.log(data)
    return (
        <div className="bg-slate-100 p-4 mb-4 w-[256px]">

        </div>
    )
}